# Service-specific overrides for Postfix master (docker-mailserver)
## Define full submission service stanza to ensure overrides are applied
submission inet n       -       y       -       -       smtpd
	-o syslog_name=postfix/submission
	-o smtpd_tls_security_level=none
	-o smtpd_tls_auth_only=no
	-o smtpd_sasl_auth_enable=yes
	-o milter_macro_daemon_name=ORIGINATING
# Removed service-specific restrictions; global main.cf will govern client/recipient restrictions.
